services:
  - type: web
    name: voxcraft
    runtime: python
    buildCommand: |
      pip install -r requirements.txt
      apt-get update && apt-get install -y ffmpeg
    startCommand: python server.py
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
