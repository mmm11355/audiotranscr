services:
  - type: web
    name: voxcraft
    runtime: python
    buildCommand: |
      pip install yt-dlp
      apt-get update && apt-get install -y ffmpeg
    startCommand: python server.py
    envVars:
      - key: PORT
        value: 10000
